(executable
  (name Run)
  (public_name Run)
  (libraries
   base
   sihl
   sihl_postgresql
   sihl_admin
   sihl_session
   sihl_session_postgresql
   sihl_email
   sihl_email_postgresql
   sihl_user
   sihl_user_postgresql
   sihl_example_issues
   caqti-driver-postgresql
  )
)
