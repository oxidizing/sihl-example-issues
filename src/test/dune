(test
 (name test)
 (libraries
  base
  sihl
  sihl_postgresql
  sihl_admin
  sihl_session
  sihl_session_postgresql
  sihl_email
  sihl_email_postgresql
  sihl_user
  sihl_user_postgresql
  sihl_example_issues
  alcotest
  alcotest-lwt
  )
 (preprocess
  (pps
   ppx_string_interpolation
  )))
